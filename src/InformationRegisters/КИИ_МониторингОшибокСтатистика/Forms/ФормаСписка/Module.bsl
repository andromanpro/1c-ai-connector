#Область ОбработчикиКомандФормы

// Переход к записям журнала мониторинга по хешу выбранной строки статистики.
&НаКлиенте
Процедура ПоказатьОшибку(Команда)

	Данные = Элементы.Список.ТекущиеДанные;
	Если Данные = Неопределено Тогда
		ПоказатьОповещениеПользователя(НСтр("ru = 'Выберите строку статистики'"));
		Возврат;
	КонецЕсли;

	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("Отбор", Новый Структура("ХешОшибки", Данные.ХешОшибки));
	ОткрытьФорму("РегистрСведений.КИИ_МониторингОшибокЖурнал.Форма.ФормаСписка",
		ПараметрыФормы, ЭтотОбъект);

КонецПроцедуры

// Глубокий инструментальный анализ ошибки по хешу выбранной строки статистики.
&НаКлиенте
Процедура ГлубокийАнализ(Команда)

	Данные = Элементы.Список.ТекущиеДанные;
	Если Данные = Неопределено Тогда
		ПоказатьОповещениеПользователя(НСтр("ru = 'Выберите строку статистики'"));
		Возврат;
	КонецЕсли;

	РезультатАнализа = ГлубокийАнализНаСервере(Данные.ХешОшибки);

КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервереБезКонтекста
Функция ГлубокийАнализНаСервере(Знач ХешОшибки)

	Возврат КИИ_МониторингОшибокАнализ.ВыполнитьГлубокийАнализОшибки(ХешОшибки).Текст;

КонецФункции

#КонецОбласти
